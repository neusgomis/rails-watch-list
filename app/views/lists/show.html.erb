<h3><%= @list.name %> List</h3>

<div class = "cards">
  <% if @list.bookmarks.empty?%>
    <p>No movies in this list<p>
  <% else %>
    <% @list.bookmarks.each do |bookmark| %>
      <div class = "card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%=bookmark.movie.poster_url%>)">
        <p><%= bookmark.movie.title %></p><br>
        <%= link_to '<i class="fa-solid fa-trash-can"></i>'.html_safe, bookmark_path(bookmark), class:'remove', method: :delete, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></a>
      </div>
    <% end %>
  <% end %>
</div>

<button class="btn btn-primary"><%= link_to "Add Movies", new_list_bookmark_path(@list)%></button>

<button class="btn btn-primary"><%= link_to "Go back to your lists", lists_path%></button>

<button class="btn btn-info"><%= link_to "Edit List", edit_list_path%></button>

<button class="btn btn-danger"><%= link_to "Delete List".html_safe, list_path(@list), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
